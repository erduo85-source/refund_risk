# 用户端补款原型

本文件夹仅包含手机端用户补款原型，与退款风控后台文件分开管理。

## 文件说明

- `用户补款.html`：第一版用户端原型。
- `用户补款第二版.html`：当前迭代版本，建议优先查看。
- `assets/game-login-bg.png`：登录页面使用的本地游戏背景图。

## 本地运行

直接双击任意 HTML 文件即可运行，无需安装依赖或联网。

请保留 HTML 文件与 `assets` 文件夹的当前相对位置，否则登录页面背景图将无法正常显示。
