# 退款风控原型：发现与决策

## 当前需求

- 优化已有退款风控原型，修复用户后续指出的瑕疵，使其达到需求评审标准。
- 用户端目录：`用户端原型/`。
- 后台目录：`退款风控后台原型_全版本/`。
- 用户端核心链路：退款通知触发规则 → 自动封禁 → 登录拦截并引导网页补款 → 支付指定金额 → 自动解封。
- 后台核心能力：封禁规则配置、退款订单管理、风险账号管理。
- 本轮交付：生成执行计划 Markdown 文件，建立后续完整变更记录，并上传至 `https://github.com/erduo85-source/refund_risk.git`。

## 已发现的项目状态

| 项目 | 发现 |
|---|---|
| Git 仓库 | 当前目录已初始化 Git，但尚无提交 |
| 当前分支 | `master` |
| Git 远端 | 尚未配置 |
| 工作区 | 用户端与后台两个原型目录均为未跟踪内容 |
| 既有计划文件 | 检查时不存在 `task_plan.md`、`findings.md`、`progress.md` |
| 用户端实现 | 2 个单文件 HTML 原型 + 1 张本地背景图；无依赖，可直接打开；README 指定 `用户补款第二版.html` 为当前版本 |
| 后台实现 | 当前实际文件为 `index.html`、`styles.css`、`app.js`、`README.md`；无外部依赖，可直接打开 |
| 提交范围审计 | 共 11 个待入库文件（含 3 个计划文件）；未发现 `node_modules`、构建产物、疑似密钥/环境文件或 ≥10 MB 大文件 |
| 文档一致性 | 后台 README 提到 `版本2.html`、`版本3.html`、`版本4.html`，但当前目录未发现这些文件；`index.html` 标题为“版本4”，后续审计时需修正文档或文件说明 |
| 目标远端 | `git ls-remote` 返回成功且无引用，目标 GitHub 仓库为空，不存在需合并或覆盖的远端历史 |
| GitHub 身份 | 已重新认证账号 `erduo85-source`；公开名称 `ZaneZan`、公开邮箱 `erduo85@gmail.com`，令牌具备 `repo` 权限 |
| 本地提交身份 | 仅在本仓库配置为 `ZaneZan <erduo85@gmail.com>`，未改动全局 Git 身份 |
| Git 基线 | 首个提交为 `b8fc247`（`chore: establish refund risk prototype baseline`） |
| 远端同步 | 已配置 `origin` 为目标仓库，并成功建立、跟踪 `origin/master` |

## 技术与流程决策

| 决策 | 原因 |
|---|---|
| 使用 `task_plan.md` 作为阶段计划 | 让范围、状态、验收标准持续可见 |
| 使用本文件记录发现、约束和决策 | 避免跨会话丢失已确认信息 |
| 使用 `progress.md` 记录所有实际变更 | 满足“后续完整记录所有变更”的可追溯要求 |
| 初次上传前先清点目录和敏感/生成文件 | 当前全部项目文件尚未纳入 Git，需要避免错误提交缓存、凭据或无关产物 |

## 待确认/待发现

- 当前可演示页面与核心流程完成度。
- 用户后续逐项指出的具体瑕疵及优先级。

## 2026-08-10 / 受控用户页批注结论

| 批注 | 处理结论 |
|---|---|
| 顶部操作 | 移除“导出名单”“手动封禁”按钮 |
| 页面说明 | 改为“管理退款用户，支持提前封禁和手动解封” |
| 指标卡 | 整行 4 张指标卡全部移除 |
| 筛选条件 | 移除风险等级筛选，仅保留用户搜索和风控状态 |
| 表格列 | 移除风险列、勾选列和全部批量操作 |
| 用户状态 | 仅保留“观察中、封禁中、已解封”；原“待补款”用户并入“封禁中” |
| 用户展示 | 仅移除列表用户列头像；详情抽屉头像保留 |
| 单用户操作 | 保留逐行详情、提前封禁与手动解封，满足新页面说明的操作语义 |
| 表格兼容 | 动态“最近操作人”列仍插入到操作列前；空状态 `colspan` 随删列同步调整 |

### 代码归属

- 当前浏览器页面由 `退款风控后台原型_全版本/index.html` 内联 CSS/JS 直接生成。
- 同目录 `styles.css`、`app.js` 未被当前 `index.html` 引用，本轮不修改。

### 验证约束

- in-app Browser 已连接到用户当前标签页，但运行时安全策略拒绝刷新 `file://` 页面；未使用本地服务器、其他浏览器或底层调试接口绕过限制。
- 可执行静态验证已覆盖：内联脚本语法、用户状态集合、基础表头数、旧筛选/批量选择器残留、用户页指标卡残留和 Git 空白错误。
- 本轮真实刷新后的视觉截图与交互回归仍需在允许访问的页面环境中完成。
- 本批次功能与留痕提交为 `fafbbe0`（`fix: simplify controlled users page`），已推送至 `origin/master`。

## 2026-08-10 / 退款记录页批注结论

| 范围 | 处理结论 |
|---|---|
| 页面说明与操作 | 说明改为“支持查询历史退款订单，快速定位退款风险”；移除“立即同步”“导出记录” |
| 页面指标 | 用现有 4 卡指标样式替换渠道链路横幅：封禁中帐号数、今日退款订单数、今日退款金额、今日补款金额 |
| 关联用户 | 第一行展示 SDKID、第二行展示 UID；两项均提供统一复制图标 |
| 搜索 | 占位符改为“请输入订单号/UID/SDKID”，查询逻辑覆盖三个字段 |
| 校验状态 | 整列删除 |
| 渠道筛选 | 从筛选表单移到列表工具栏，复用受控用户页分段按钮样式与交互 |
| 订单号 | 订单号后增加统一复制图标 |
| 时间筛选 | 支持近 3/7/30 天快捷项，以及自定义开始/结束日期时间 |
| 风控结果 | 两行展示；结果仅为“观察中 / 账号封禁”，详情按累计订单数或金额解释 |
| 操作 | 保留“查看关联用户”，新增“查看订单详情”文字按钮和订单详情抽屉 |

### 本轮实现决策

- 为 8 条 mock 用户数据补充稳定的 `sdkId`，现有 `id` 继续作为 UID。
- 已解封用户的历史退款风控结果显示“账号封禁”，当前状态仍在用户详情中显示“已解封”。
- 日期范围组件使用原生 `datetime-local` 输入与项目内弹层样式，不新增第三方日期库；快捷区间以当前 mock 数据最新退款时间为结束时间。
- 指标卡复用现有 `metricCard()` 和响应式断点；退款筛选区单独设置桌面三列、≤1180px 两列、窄屏单列。
- 退款页汇总指标沿用项目级总量，不用仅展示的 8 条 mock 明细反推帐号总数；因此“封禁中帐号数”与项目当前风险帐号总数保持一致。

### 本轮验证结论

- 内联脚本已由 Node.js 解析通过；12 项新旧标记、7 列表头、8 个 SDKID、复制与订单详情入口静态断言全部通过。
- `git diff --check` 无空白错误。
- in-app Browser 对当前 `file://` 页面仍无法自动刷新，因此真实视觉、点击复制、日期弹层和详情抽屉交互尚未在刷新后的页面中验证。

## 问题记录

| 问题 | 处理 |
|---|---|
| `python.exe` 无法运行会话恢复脚本 | 改用 Git 状态和本地文件检查恢复上下文；不影响 Markdown 计划文件建立 |
| 内置补丁接口出现 Windows 沙箱刷新错误 | 连续失败后停止重复，改为直接调用环境内同一补丁执行器 |
| PowerShell 管道/批处理包装器无法正确传递 UTF-8 多行补丁 | 绕过包装器，直接向补丁执行器传参后成功 |
| `.git/index.lock` 在默认沙箱中不可写 | Git 元数据写操作改用获批的提升权限执行 |
| GitHub CLI 原令牌失效 | 完成网页设备授权，账号 `erduo85-source` 已恢复登录 |
| 批次 002 内置补丁接口再次出现 Windows 沙箱刷新错误 | 沿用上轮已验证的直接补丁执行器完成修改 |
| 超长补丁在 Windows 参数传递中被截断 | 拆分为多个小补丁，并按真实源码上下文逐块应用 |
| PowerShell 原生参数传递剥离补丁中的双引号 | 对双引号显式转义后应用成功 |
| in-app Browser 拒绝刷新 `file://` 页面 | 不绕过安全策略；改做静态验证并保留视觉复验项 |
| `C:\tmp` 无法写入临时脚本 | 使用仓库内受控临时文件完成检查，检查后已删除 |

## 资源

- 目标仓库：<https://github.com/erduo85-source/refund_risk.git>
- 本地用户端：`用户端原型/`
- 本地后台：`退款风控后台原型_全版本/`
