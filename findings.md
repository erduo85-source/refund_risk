# 退款风控原型：发现与决策

## 当前需求

- 优化已有退款风控原型，修复用户后续指出的瑕疵，使其达到需求评审标准。
- 用户端目录：`用户端原型/`。
- 后台目录：`退款风控后台原型_全版本/`。
- 用户端核心链路：退款通知触发规则 → 自动封禁 → 登录拦截并引导网页补款 → 支付指定金额 → 自动解封。
- 后台核心能力：封禁规则配置、退款订单管理、风险账号管理。
- 本轮交付：生成执行计划 Markdown 文件，建立后续完整变更记录，并上传至 `https://github.com/erduo85-source/refund_risk.git`。

## 已发现的项目状态

| 项目 | 发现 |
|---|---|
| Git 仓库 | 当前目录已初始化 Git，但尚无提交 |
| 当前分支 | `master` |
| Git 远端 | 尚未配置 |
| 工作区 | 用户端与后台两个原型目录均为未跟踪内容 |
| 既有计划文件 | 检查时不存在 `task_plan.md`、`findings.md`、`progress.md` |
| 用户端实现 | 2 个单文件 HTML 原型 + 1 张本地背景图；无依赖，可直接打开；README 指定 `用户补款第二版.html` 为当前版本 |
| 后台实现 | 当前实际文件为 `index.html`、`styles.css`、`app.js`、`README.md`；无外部依赖，可直接打开 |
| 提交范围审计 | 共 11 个待入库文件（含 3 个计划文件）；未发现 `node_modules`、构建产物、疑似密钥/环境文件或 ≥10 MB 大文件 |
| 文档一致性 | 后台 README 提到 `版本2.html`、`版本3.html`、`版本4.html`，但当前目录未发现这些文件；`index.html` 标题为“版本4”，后续审计时需修正文档或文件说明 |
| 目标远端 | `git ls-remote` 返回成功且无引用，目标 GitHub 仓库为空，不存在需合并或覆盖的远端历史 |
| GitHub 身份 | 已重新认证账号 `erduo85-source`；公开名称 `ZaneZan`、公开邮箱 `erduo85@gmail.com`，令牌具备 `repo` 权限 |
| 本地提交身份 | 仅在本仓库配置为 `ZaneZan <erduo85@gmail.com>`，未改动全局 Git 身份 |
| Git 基线 | 首个提交为 `b8fc247`（`chore: establish refund risk prototype baseline`） |
| 远端同步 | 已配置 `origin` 为目标仓库，并成功建立、跟踪 `origin/master` |

## 技术与流程决策

| 决策 | 原因 |
|---|---|
| 使用 `task_plan.md` 作为阶段计划 | 让范围、状态、验收标准持续可见 |
| 使用本文件记录发现、约束和决策 | 避免跨会话丢失已确认信息 |
| 使用 `progress.md` 记录所有实际变更 | 满足“后续完整记录所有变更”的可追溯要求 |
| 初次上传前先清点目录和敏感/生成文件 | 当前全部项目文件尚未纳入 Git，需要避免错误提交缓存、凭据或无关产物 |

## 待确认/待发现

- 当前可演示页面与核心流程完成度。
- 用户后续逐项指出的具体瑕疵及优先级。

## 2026-08-10 / 受控用户页批注结论

| 批注 | 处理结论 |
|---|---|
| 顶部操作 | 移除“导出名单”“手动封禁”按钮 |
| 页面说明 | 改为“管理退款用户，支持提前封禁和手动解封” |
| 指标卡 | 整行 4 张指标卡全部移除 |
| 筛选条件 | 移除风险等级筛选，仅保留用户搜索和风控状态 |
| 表格列 | 移除风险列、勾选列和全部批量操作 |
| 用户状态 | 仅保留“观察中、封禁中、已解封”；原“待补款”用户并入“封禁中” |
| 用户展示 | 仅移除列表用户列头像；详情抽屉头像保留 |
| 单用户操作 | 保留逐行详情、提前封禁与手动解封，满足新页面说明的操作语义 |
| 表格兼容 | 动态“最近操作人”列仍插入到操作列前；空状态 `colspan` 随删列同步调整 |

### 代码归属

- 当前浏览器页面由 `退款风控后台原型_全版本/index.html` 内联 CSS/JS 直接生成。
- 同目录 `styles.css`、`app.js` 未被当前 `index.html` 引用，本轮不修改。

### 验证约束

- in-app Browser 已连接到用户当前标签页，但运行时安全策略拒绝刷新 `file://` 页面；未使用本地服务器、其他浏览器或底层调试接口绕过限制。
- 可执行静态验证已覆盖：内联脚本语法、用户状态集合、基础表头数、旧筛选/批量选择器残留、用户页指标卡残留和 Git 空白错误。
- 本轮真实刷新后的视觉截图与交互回归仍需在允许访问的页面环境中完成。
- 本批次功能与留痕提交为 `fafbbe0`（`fix: simplify controlled users page`），已推送至 `origin/master`。

## 2026-08-10 / 退款记录页批注结论

| 范围 | 处理结论 |
|---|---|
| 页面说明与操作 | 说明改为“支持查询历史退款订单，快速定位退款风险”；移除“立即同步”“导出记录” |
| 页面指标 | 用现有 4 卡指标样式替换渠道链路横幅：封禁中帐号数、今日退款订单数、今日退款金额、今日补款金额 |
| 关联用户 | 第一行展示 SDKID、第二行展示 UID；两项均提供统一复制图标 |
| 搜索 | 占位符改为“请输入订单号/UID/SDKID”，查询逻辑覆盖三个字段 |
| 校验状态 | 整列删除 |
| 渠道筛选 | 从筛选表单移到列表工具栏，复用受控用户页分段按钮样式与交互 |
| 订单号 | 订单号后增加统一复制图标 |
| 时间筛选 | 支持近 3/7/30 天快捷项，以及自定义开始/结束日期时间 |
| 风控结果 | 两行展示；结果仅为“观察中 / 账号封禁”，详情按累计订单数或金额解释 |
| 操作 | 保留“查看关联用户”，新增“查看订单详情”文字按钮和订单详情抽屉 |

### 本轮实现决策

- 为 8 条 mock 用户数据补充稳定的 `sdkId`，现有 `id` 继续作为 UID。
- 已解封用户的历史退款风控结果显示“账号封禁”，当前状态仍在用户详情中显示“已解封”。
- 日期范围组件使用原生 `datetime-local` 输入与项目内弹层样式，不新增第三方日期库；快捷区间以当前 mock 数据最新退款时间为结束时间。
- 指标卡复用现有 `metricCard()` 和响应式断点；退款筛选区单独设置桌面三列、≤1180px 两列、窄屏单列。
- 退款页汇总指标沿用项目级总量，不用仅展示的 8 条 mock 明细反推帐号总数；因此“封禁中帐号数”与项目当前风险帐号总数保持一致。

### 本轮验证结论

- 内联脚本已由 Node.js 解析通过；12 项新旧标记、7 列表头、8 个 SDKID、复制与订单详情入口静态断言全部通过。
- `git diff --check` 无空白错误。
- in-app Browser 对当前 `file://` 页面仍无法自动刷新，因此真实视觉、点击复制、日期弹层和详情抽屉交互尚未在刷新后的页面中验证。
- 本批次功能与首轮留痕提交为 `9b68277`（`fix: refine refund records page`），已推送至 `origin/master`。

## 2026-08-10 / 退款策略页批注结论

| 范围 | 处理结论 |
|---|---|
| 页面说明 | 改为“精细管控账号退款行为，自动封禁与补款解封规则一键配置” |
| 页头操作 | 隐藏“变更历史”按钮 |
| 总状态信息 | 删除“生效范围”，保留最近发布与操作人 |
| 策略区标题 | 隐藏“用一句话了解当前规则，点击卡片进入完整配置”副标题 |
| 策略卡片 | 隐藏“渠道接入状态”“执行与通知”，仅保留封禁触发与补款解封两张卡 |
| 卡片时间 | 默认更新时间统一为“更新时间 2026/07/07 12:00” |

### 本轮实现决策

- 两张隐藏卡片从最终策略页渲染集合中移除，不使用 CSS 隐藏，避免仍可聚焦或被辅助技术读取。
- 仅移除策略页入口卡片；既有渠道/执行配置函数暂不删除，避免扩大到其他原型逻辑。
- 剩余两张策略卡在桌面继续采用两列容器，≤700px 沿用现有单列断点。

### 本轮验证结论

- 内联脚本由 Node.js 解析通过；最终 `renderConfigV3()` 的 7 项新旧标记静态断言全部通过。
- 桌面策略卡容器已从四列调整为两列；现有 ≤1080px 两列、≤700px 单列断点继续兼容。
- in-app Browser 对当前 `file://` 页面的 DOM 读取被 URL 策略拒绝，未绕过限制；刷新后视觉和卡片点击回归仍待手动完成。
- 本批次功能与首轮留痕提交为 `92137ce`（`fix: simplify refund policy page`），已推送至 `origin/master`。

## 2026-08-10 / 受控用户页标识与渠道布局批注结论

| 范围 | 处理结论 |
|---|---|
| 渠道 Tab | 在列表工具栏中增加弹性占位，使渠道切换整体靠右，与退款记录页一致 |
| 用户首行 | 展示加密账号名，统一为邮箱形式并在中间使用 4 个星号 |
| 用户次行 | 展示纯数字 SDKID，统一为 15 位示例数据 |
| 快捷复制 | 账号名与 SDKID 后均使用项目统一复制图标和反馈逻辑 |
| 搜索语义 | 搜索提示与匹配字段同步调整为账号名、SDKID、商店订单号，同时保留 UID 兼容匹配 |

### 本轮实现决策

- 账号名与 SDKID 写入 8 条 mock 数据，不在模板中临时拼接，确保列表、筛选和详情数据源一致。
- 用户昵称仍保留在数据模型与详情抽屉中，仅受控用户列表首列切换为账号名与 SDKID。
- 工具栏复用现有 `.toolbar-spacer`，移动端沿用既有规则隐藏占位并让渠道 Tab 独占一行。
- Node 静态校验已通过：1 段内联脚本可解析，8 个 SDKID 均为唯一 15 位数字，8 个账号均符合中间 4 个星号的邮箱格式；复制入口、展示顺序、搜索字段和渠道对齐结构均命中。
- in-app Browser 对当前 `file://` 页面的 DOM 读取仍被 URL 策略拒绝，未绕过限制；刷新后视觉、复制反馈与渠道切换仍待手动完成。
- 本批次功能与首轮留痕提交为 `7333219`（`fix: refine controlled user identifiers`），已推送至 `origin/master`。

## 2026-08-10 / 后台公共导航批注结论

| 范围 | 处理结论 |
|---|---|
| 左侧导航 | 删除“风险概览”入口，保留受控用户、退款记录、退款策略和操作日志四项业务菜单 |
| 默认落页 | 沿用现有 `state.page = "users"` 和受控用户激活态，删除首项菜单后不会出现无激活菜单或空白页 |
| 顶部导航 | 将一级模块名称“风控管理”统一改为“退款风控”，对应点击反馈标题同步更新 |
| 页面实现 | 风险概览页面源码暂保留为不可导航的内部原型内容，本轮不扩大为页面与样式整体删除 |

- Node 静态校验已通过：1 段内联脚本可解析，侧栏菜单为 4 项且无 `overview` 入口，受控用户保持默认页及激活态，“风控管理”旧文案为 0。
- in-app Browser 对当前 `file://` 页面的 DOM 读取仍被 URL 策略拒绝，未绕过限制；导航视觉与顶部文案仍待本地刷新复验。
- 本批次功能与首轮留痕提交为 `6d92606`（`fix: simplify refund risk navigation`），已推送至 `origin/master`。

## 2026-08-10 / 退款策略卡密度批注结论

| 范围 | 处理结论 |
|---|---|
| 桌面端 | 策略卡网格固定为四列；当前仅展示两张卡时各占四分之一内容区，不再横向拉伸至半屏 |
| 平板端 | 沿用 ≤1080px 两列断点，保证卡片内容仍有足够阅读宽度 |
| 手机端 | 沿用 ≤700px 单列断点，避免窄屏卡片过小 |
| CSS 一致性 | 同步修改两处同名基础规则，避免早期样式段与最终覆盖段配置相互矛盾 |

- Node 静态校验已通过：两处桌面基础规则均为四列，平板两列和手机单列断点均命中，最终 CSS 覆盖顺序正确，内联脚本可解析。
- in-app Browser 对当前 `file://` 页面的 DOM 读取仍被 URL 策略拒绝，未绕过限制；1647px 桌面视觉与两个响应式断点仍待本地人工复验。
- 本批次功能与首轮留痕提交为 `52b2579`（`fix: compact refund policy cards`），已推送至 `origin/master`。

## 问题记录

| 问题 | 处理 |
|---|---|
| `python.exe` 无法运行会话恢复脚本 | 改用 Git 状态和本地文件检查恢复上下文；不影响 Markdown 计划文件建立 |
| 内置补丁接口出现 Windows 沙箱刷新错误 | 连续失败后停止重复，改为直接调用环境内同一补丁执行器 |
| PowerShell 管道/批处理包装器无法正确传递 UTF-8 多行补丁 | 绕过包装器，直接向补丁执行器传参后成功 |
| `.git/index.lock` 在默认沙箱中不可写 | Git 元数据写操作改用获批的提升权限执行 |
| GitHub CLI 原令牌失效 | 完成网页设备授权，账号 `erduo85-source` 已恢复登录 |
| 批次 002 内置补丁接口再次出现 Windows 沙箱刷新错误 | 沿用上轮已验证的直接补丁执行器完成修改 |
| 超长补丁在 Windows 参数传递中被截断 | 拆分为多个小补丁，并按真实源码上下文逐块应用 |
| PowerShell 原生参数传递剥离补丁中的双引号 | 对双引号显式转义后应用成功 |
| in-app Browser 拒绝刷新 `file://` 页面 | 不绕过安全策略；改做静态验证并保留视觉复验项 |
| `C:\tmp` 无法写入临时脚本 | 使用仓库内受控临时文件完成检查，检查后已删除 |
| 批次 005 两次内联 Node 命令被 PowerShell 剥离引号 | 未写入源码；改用仓库内临时校验脚本执行，通过后已用补丁删除 |
| 批次 005 in-app Browser 拒绝读取 `file://` 页面 | 未重复尝试或绕过安全策略；完成静态校验并保留本地人工复验项 |
| 批次 006 内联校验中的中文正则和路径被 PowerShell 转码 | 两次校验均未写入源码；改为 ASCII 路径发现与 Unicode 转义后通过 |
| 批次 006 in-app Browser 拒绝读取 `file://` 页面 | 未重复尝试或绕过安全策略；完成静态校验并保留本地人工复验项 |
| 批次 007 in-app Browser 拒绝读取 `file://` 页面 | 未重复尝试或绕过安全策略；完成 CSS 结构校验并保留本地人工复验项 |

## 2026-08-10 / 受控用户详情抽屉批注结论

| 范围 | 处理结论 |
|---|---|
| 信息条带 | 账号信息改为蓝色主题条带；移除头像；首行展示加密账号名，次行展示 UID，两行均复用项目统一复制图标与反馈逻辑 |
| 汇总指标 | “已补款”统一改为“累计补款”，继续展示累计退款与仍需补款 |
| Tab 命名 | 调整为“账号信息 / 风险轨迹 / 订单信息”，并补齐 tab、tabpanel 的 ARIA 关联 |
| 风险轨迹 | 采用垂直时间轴，区分自动封禁/解封、玩家补款、商店退款和资产核查事件，展示时间、详情及正负金额 |
| 订单信息 | 按用户累计退款单数生成多行订单列表；订单号支持复制，展示金额、渠道、下单时间和退款时间 |
| 底部操作 | 独立“复制 UID”按钮移除；封禁/解封操作固定在抽屉底部左侧，不随正文滚动 |

### 设计参考与适配决策

- 风险时间轴参考 Figma 节点 `2617:1936`：采用 10px 节点、1px 连接线及“事件 / 时间 / 详情 / 金额”信息层级；事件语义替换为退款风控原型现有状态。
- 订单列表参考 Figma 节点 `2617:1879`：复用浅蓝汇总提示与多行表格结构；因现有详情抽屉宽度为 560px，表格最小宽度设为 720px，并在内容区域提供横向滚动，避免压缩字段或异常换行。
- 抽屉正文与操作区采用纵向 Flex：标题固定、正文独立滚动、底栏不收缩；此结构只在用户详情模式生效，退款订单详情抽屉仍沿用原布局。
- 未新增图片或外部依赖；复制、状态标签、金额格式化及封禁/解封操作均复用项目现有实现。

### 本轮验证结论

- Node.js 已解析 1 段内联脚本；12 项静态断言全部通过，覆盖 Tab、累计补款、无头像、复制入口、蓝色条带、固定底栏、垂直时间轴、多订单列表和抽屉模式生命周期。
- `git diff --check` 无空白错误。
- in-app Browser 对本地 `file://` 页面的自动读取受 URL 策略限制；刷新后的视觉、横向滚动、复制反馈和底栏固定效果仍需本地人工复验。
- 本批次功能与首轮留痕提交为 `ebb0d58`（`fix: refine controlled user drawer`），已推送至 `origin/master`。

## 2026-08-10 / 退款查询交互、订单详情与用户筛选批注结论

| 范围 | 处理结论 |
|---|---|
| Figma 连接 | 重新授权后账号 `耳朵 / erduo85@gmail.com` 可正常访问指定文件；节点 `2618:2240`、`2618:3378` 的结构与截图均成功读取 |
| 退款时间输入 | 改为开始、结束时间分段展示，保留近 3 天、近 7 天、近 30 天快捷选项 |
| 日期时间面板 | 按 Figma 构建月历、年月切换、范围高亮、时/分/秒三列选择和取消/确定操作；提交前校验开始时间不晚于结束时间 |
| 退款订单详情 | 改为宽抽屉与“账号信息 / 退款信息”两块边框分区，每块采用三列字段布局；新增退款时间字段并保留统一复制入口 |
| 用户筛选 | 筛选项统一为账号、帐号状态、补款状态；账号仅匹配 SDKID 或账号名，补款状态按未补款、部分补款、补款完成计算 |
| 响应式 | 用户筛选在桌面为三项筛选加操作区，≤1180px 两列、≤900px 单列；订单详情在 ≤900px 两列、≤620px 单列；日期面板窄屏允许横向滚动 |

### 设计参考与实现决策

- 时间输入依据 Figma 节点 `2618:2240` 的 32px 输入高度、浅灰边框、2px 圆角、日历图标及“开始时间 → 结束时间”层级实现。
- 时间弹层依据 Figma 节点 `2618:3378` 的 280px 月历区、184px 时间区、40px 头部、24px 日期单元格和底部操作区实现，并适配本原型的蓝色主题变量。
- Figma 临时 SVG 资源地址在当前环境返回空内容或连接失败；未引用可能过期的外部资源，日历图标采用 CSS 绘制，翻页箭头复用项目现有图标。这是本轮唯一可见实现差异。
- 订单详情按用户提供的参考图重构为信息分组抽屉；字段仍来自现有 mock 数据，不新增后端或接口依赖。
- 补款状态口径：`supplement <= 0` 为未补款，`0 < supplement < refund` 为部分补款，`supplement >= refund` 为补款完成。
- 筛选值写入页面状态，查询、渠道切换导致重新渲染后仍保持当前条件；重置会同时清空三项条件和渠道条件。

### 本轮验证结论

- Figma 连接与两个指定节点读取均通过。
- Node.js 已解析 1 段内联脚本（111272 字节）；用户筛选、选项枚举、四条件联合过滤、日期时间面板、秒级边界和订单详情 6 组静态断言全部通过。
- `git diff --check` 无空白错误。
- in-app Browser 对本地 `file://` 页面刷新受 URL 策略限制，未重试或绕过；刷新后的像素表现、日期选择、筛选联动和抽屉响应式仍需本地人工复验。
- 功能提交：`c4661e8`（`fix: refine refund query interactions`）。

## 2026-08-10 / 操作日志筛选批注结论

| 范围 | 处理结论 |
|---|---|
| 操作类型 | 下拉枚举统一为策略修改、账号封禁、账号解封、补款金额修改 |
| 操作人 | 保持单选下拉，提供全部、系统、林晓、周岚 |
| 操作时间 | 复用退款记录页日期时间面板的样式与交互，支持起止日期、时分秒和近 3/7/30 天快捷选项 |
| 查询逻辑 | 操作类型、操作人、操作时间三项联合过滤，列表标题实时展示结果数，无结果时显示引导空状态 |
| 日志数据 | 统一现有 mock 日志的操作类型命名，移除筛选枚举之外的“渠道重试”等类型，保证列表与筛选口径一致 |
| 响应式 | 桌面端三项筛选加操作区；≤1180px 两列并让按钮独占一行；≤700px 单列 |

### 本轮实现决策与验证

- 删除“搜索日志”输入框，不保留隐性文本筛选，严格按用户指定的三个筛选项实现。
- 日期时间组件复用既有 CSS、范围高亮和面板布局，但使用独立的日志状态、DOM ID 和事件属性，避免与退款记录页互相污染。
- 查询按钮真正更新数据源；快捷时间、确定自定义区间后会立即刷新日志列表并显示 Toast 结果。
- Node.js 已解析 1 段内联脚本（124890 字节）；三项筛选、操作类型枚举、操作人单选、日期时间面板、联合过滤、事件绑定、响应式和结果反馈 9 项断言全部通过。
- `git diff --check` 无空白错误。
- in-app Browser 对本地 `file://` 页面刷新受 URL 策略限制，未重试或绕过；刷新后的视觉、日期弹层和筛选点击链路仍需本地人工复验。
- 功能提交：`937b876`（`fix: refine audit log filters`）。

## 2026-08-10 / 操作日志页头按钮批注结论

- “导出日志”从操作日志页的 `pageHeading()` 操作参数中直接移除，不使用 CSS 隐藏，因此不会保留不可见焦点或无效点击入口。
- 该改动只影响操作日志页头；其他页面已有的导出或下载入口不做全局修改。
- 不涉及断点或容器适配：页头无操作区后由现有 `pageHeading` 结构自然只渲染标题与说明。
- 内联脚本解析通过；按钮文案、`data-action="export"` 和导出图标在 `renderAudit()` 片段中均为 0；`git diff --check` 通过。
- 当前 `file://` 页面仍受已知浏览器 URL 策略限制，本轮未重复尝试被禁止的自动刷新；需手动刷新确认页头留白。
- 功能提交：`95cd9de`（`fix: remove audit export action`）。

## 2026-08-10 / 受控用户账号信息字段批注结论

| 范围 | 处理结论 |
|---|---|
| 字段清单 | 账号信息 Tab 固定展示 SDKID、账号名、UID、退款渠道、最近登录时间、最近登录设备、最近登录 IP 共 7 项；移除该面板中的触发规则和首次退款 |
| 示例数据 | 首条账号更新为 `67***832@qq.com`，SDKID 更新为 `1000128138132`；显示 UID 使用 `u129312301320130` 形式，登录 IP 使用“IP + 地区”形式 |
| 多值展示 | 账号涉及多个退款角色时，UID 使用顿号连接；涉及多个商店时，退款渠道同样使用顿号连接 |
| 复制能力 | SDKID、账号名继续复用项目统一复制图标与 Toast 反馈；顶部蓝色身份条带同步展示并复制完整 UID 文本 |
| 数据建模 | 保留 `U-...` 作为内部关联主键，新增独立的账号画像映射承载展示 UID、退款渠道集合和最近登录 IP，避免修改订单及操作关联键 |

### 本轮验证结论

- Node.js 已解析全部内联脚本；字段顺序、旧字段移除、复制入口、多 UID、多渠道、IP 和示例格式共 9 项断言全部通过。
- `git diff --check` 无空白错误。
- 已知 in-app Browser URL 策略禁止自动刷新当前 `file://` 页面，本轮未重复尝试或绕过；抽屉实际换行、复制反馈和多值视觉仍需本地手动刷新复验。
- 功能提交：`37c1aa5`（`fix: refine user account profile`）。

## 2026-08-10 / 解除封禁确认弹窗批注结论

| 范围 | 处理结论 |
|---|---|
| 账号信息 | 弹窗信息区改为更明显的浅蓝背景、蓝色描边与左侧主题色条；首行显示账号名和 SDKID，次行显示累计退款和累计补款 |
| 补款金额 | 仅解除封禁时显示必填金额输入，占位为“请输入账号已补款的金额”；封禁确认弹窗不显示该字段 |
| 输入校验 | 空值、负数、非数字及超过两位小数均阻止提交并显示行内错误；允许填写 0，以兼容已核验申诉但无补款的人工解封场景 |
| 状态更新 | 确认解封后将输入值写入账号累计补款，刷新用户列表并在成功 Toast 中回显更新后的金额 |
| 响应式 | 两行账号信息使用 Flex 自动换行，不新增固定宽度或额外断点，沿用现有 460px 弹窗与手机端输入高度规则 |

### 本轮验证结论

- Node.js 已解析全部内联脚本；视觉标识、两行信息、必填字段、精确占位、解封专属显示、金额校验、数据更新、焦点和反馈共 13 项断言全部通过。
- `git diff --check` 无空白错误。
- in-app Browser 已连接并接管当前原型页，但 URL 策略拒绝读取本地 `file://` DOM；未重试或绕过，弹窗视觉、输入焦点与提交反馈仍需手动刷新复验。
- 功能提交：`3ad4fde`（`fix: refine unblock confirmation modal`）。

## 2026-08-10 / 补款解封规则配置批注结论

| 范围 | 处理结论 |
|---|---|
| 补款方式 | 配置项收敛为“联系客服补款”和“网页自助补款”；默认选择网页自助补款，以匹配用户端自主补款主链路 |
| 补款地址 | 仅网页自助补款时展示并必填；提供提示“补款地址可在充值中心后台配置网页支付后获取”，发布前校验为有效的 http/https 地址 |
| 解封规则 | 独立提供“补款后自动解封”和“补款后人工审核”两项，保存后保留当前选择 |
| 配置精简 | 活跃抽屉只渲染“补款方式”和“解封规则”两个区块，不再展示补款金额计算、重复退款处理、二次退款加重等旧配置 |
| 演示数据 | 默认地址 `https://pay.example.com/refund/nebula` 仅用于本地原型展示，不代表真实充值中心地址 |

### 本轮验证结论

- Node.js 已解析全部内联脚本；两类补款方式、条件地址、精确提示、两类解封规则、旧配置移除、联动、URL 校验、保存、摘要和属性转义共 16 项断言全部通过。
- `git diff --check` 无空白错误。
- 根据浏览器技能与既有 URL 策略结论，本轮未重复请求被禁止的本地 `file://` DOM 读取；抽屉视觉、单选联动、错误提示及发布后回显仍需手动刷新复验。
- 功能提交：`b2716af`（`fix: refine recovery policy settings`）。

## 2026-08-10 / 封禁触发规则配置批注结论

| 范围 | 处理结论 |
|---|---|
| 规则范围 | 活跃抽屉仅保留单笔退款金额、累计退款金额、累计退款笔数三项封禁规则，不再展示低额退款豁免或资产消耗等配置 |
| 条件关系 | 新增“满足任一条件即封禁”和“满足所有条件才封禁”；关系仅作用于当前已启用规则 |
| 统计周期 | 使用一个全局账号统计周期，同时作用于累计退款金额和累计退款笔数；单笔退款金额继续按单笔订单判断 |
| 周期选项 | 支持近 7、30、60、180、360 天，默认近 30 天，并实时提示“统计该账号近 N 天的退款订单” |
| 状态保存 | 保存规则关系、统计周期、三个阈值及各规则启用状态；配置卡摘要与自然语言说明同步更新 |

### 本轮验证结论

- Node.js 已解析全部内联脚本；配置结构、三条规则、旧配置移除、条件关系、统一周期、阈值与开关、联动、保存和摘要共 20 项断言全部通过。
- `git diff --check` 无空白错误。
- 根据浏览器技能与既有 URL 策略结论，本轮未重复请求被禁止的本地 `file://` DOM 读取；抽屉布局、关系切换、周期提示和保存回显仍需手动刷新复验。
- 功能提交：`7b5541d`（`fix: refine blocking trigger policy`）。

## 2026-08-10 / 补款地址布局收敛批注结论

- 移除补款地址原有的独立浅蓝背景、完整边框、圆角和外层缩进，避免“配置卡 → 选项卡 → 地址卡”的多层容器。
- 联系客服、网页自助两个补款方式改为显式单选控件与标签结构；网页自助选项内部直接承载地址标签、输入框、提示和错误信息。
- 地址区仅使用一条顶部浅蓝分隔线与选项说明区分层级，不再形成新的卡片。
- 地址字段 ID、帮助提示、条件显示、必填切换、URL 校验、错误反馈及保存状态保持不变。
- Node.js 内联脚本解析、结构层级、旧样式移除、语义标签和逻辑保留共 12 项断言全部通过；`git diff --check` 通过。
- 本轮未重复请求已被 URL 策略禁止的本地 `file://` DOM 读取；实际选项高度、输入聚焦和切换动画仍需手动刷新复验。
- 功能提交：`f033508`（`fix: flatten recovery address layout`）。

## 资源

- 目标仓库：<https://github.com/erduo85-source/refund_risk.git>
- 本地用户端：`用户端原型/`
- 本地后台：`退款风控后台原型_全版本/`
